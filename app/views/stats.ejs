<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Stats - <%= link.shortCode %>
    </title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Google Material Icons -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
    </style>
</head>

<body class="bg-gray-100 min-h-screen flex justify-center py-10">

    <div class="bg-white w-full max-w-3xl shadow-2xl rounded-2xl p-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-6 flex items-center gap-2">
            <span class="material-icons text-indigo-600">analytics</span>
            URL Detailed Stats
        </h1>

        <!-- TABLE -->
        <table class="w-full border-collapse overflow-hidden rounded-xl shadow-sm">
            <tbody class="text-gray-700 text-lg">

                <tr class="border-b hover:bg-gray-50">
                    <td class="p-4 font-semibold">Original URL</td>
                    <td class="p-4 text-blue-600 underline break-all">
                        <a href="<%= link.longUrl %>" target="_blank">
                            <%= link.longUrl %>
                        </a>
                    </td>
                </tr>

                <tr class="border-b hover:bg-gray-50">
                    <td class="p-4 font-semibold">Short Code</td>
                    <td class="p-4">
                        <%= link.shortName %>
                    </td>
                </tr>

                <tr class="border-b hover:bg-gray-50">
                    <td class="p-4 font-semibold">Total Clicks</td>
                    <td class="p-4 flex items-center gap-2">
                        <span class="material-icons text-pink-500">touch_app</span>
                        <%= link.clickCount %>
                    </td>
                </tr>

                <tr class="border-b hover:bg-gray-50">
                    <td class="p-4 font-semibold">Created On</td>
                    <td class="p-4">
                        <%= link.createdAt.toLocaleString() %>
                    </td>
                </tr>

                <tr class="hover:bg-gray-50">
                    <td class="p-4 font-semibold">Last Updated</td>
                    <td class="p-4">
                        <%= link.lastClickedTime.toLocaleString() %>
                    </td>
                </tr>

            </tbody>
        </table>

        <!-- Back Button -->
        <div class="text-center mt-8">
            <a href="/"
                class="inline-flex items-center gap-2 px-6 py-3 bg-indigo-600 text-white font-semibold rounded-xl shadow-lg hover:bg-indigo-700 transition">
                <span class="material-icons">arrow_back</span>
                Back to Dashboard
            </a>
        </div>

    </div>

</body>

</html>